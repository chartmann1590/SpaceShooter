name: CI/CD Pipeline

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

env:
  JAVA_VERSION: '17'

jobs:
  # =========================================
  # JOB 1: Build APK
  # =========================================
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.tag }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    
    - name: Build debug APK
      run: ./gradlew assembleDebug --no-daemon
    
    - name: Build release APK (if signed)
      run: ./gradlew assembleRelease --no-daemon || echo "Release build skipped (no keystore)"
    
    - name: Extract version tag
      id: version
      run: |
        if [[ "${{ github.ref }}" == refs/tags/* ]]; then
          echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          echo "is_release=true" >> $GITHUB_OUTPUT
        else
          echo "tag=build-${{ github.run_number }}" >> $GITHUB_OUTPUT
          echo "is_release=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Upload debug APK
      uses: actions/upload-artifact@v4
      with:
        name: apk-debug
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 5
    
    - name: Upload release APK
      if: steps.version.outputs.is_release == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: apk-release
        path: app/build/outputs/apk/release/app-release-unsigned.apk
        retention-days: 30
    
    - name: Verify build
      run: |
        if [ -f app/build/outputs/apk/debug/app-debug.apk ]; then
          echo "✓ Build successful!"
          ls -lh app/build/outputs/apk/debug/app-debug.apk
        else
          echo "✗ Build failed!"
          exit 1
        fi

  # =========================================
  # JOB 2: E2E Tests
  # =========================================
  e2e-tests:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Download APK
      uses: actions/download-artifact@v4
      with:
        name: apk-debug
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Boot Android Emulator
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 34
        target: google_apis
        arch: x86_64
        profile: Nexus 6
        emulator-options: -no-snapshot -noaudio -no-window -gpu swiftshader_indirect
        script: |
          echo "Waiting for emulator to boot..."
          sleep 60
          echo "Emulator ready"
    
    - name: Wait for device
      run: adb wait-for-device
    
    - name: Install APK
      run: adb install -r app/build/outputs/apk/debug/app-debug.apk
    
    - name: Launch app
      run: |
        adb shell am start -n com.spaceshooter/.MainActivity
        sleep 5
    
    - name: Run E2E Tests
      run: |
        echo "=========================================="
        echo "E2E SMOKE TEST"
        echo "=========================================="
        
        echo ""
        echo "1. Checking if app is installed..."
        PACKAGE_CHECK=$(adb shell pm list packages | grep com.spaceshooter)
        if [ -n "$PACKAGE_CHECK" ]; then
          echo "   ✓ PASS: App is installed"
        else
          echo "   ✗ FAIL: App not installed"
          exit 1
        fi
        
        echo ""
        echo "2. Checking if app is running..."
        PROCESS_CHECK=$(adb shell ps -A | grep com.spaceshooter)
        if [ -n "$PROCESS_CHECK" ]; then
          echo "   ✓ PASS: App is running"
        else
          echo "   ✗ FAIL: App not running"
          exit 1
        fi
        
        echo ""
        echo "3. Taking screenshot..."
        adb shell screencap -p /sdcard/screen.png
        adb pull /sdcard/screen.png ./
        echo "   ✓ Screenshot saved"
        
        echo ""
        echo "4. Checking for crashes..."
        adb logcat -d -t 100 > ./logcat.txt
        CRASHES=$(grep -i "FATAL\|crash\|exception" ./logcat.txt | grep com.spaceshooter | head -5)
        if [ -z "$CRASHES" ]; then
          echo "   ✓ PASS: No crashes detected"
        else
          echo "   ✗ FAIL: Crashes found:"
          echo "$CRASHES"
          exit 1
        fi
        
        echo ""
        echo "=========================================="
        echo "GAME INTERACTION TEST"
        echo "=========================================="
        
        echo ""
        echo "5. Restarting app..."
        adb shell am force-stop com.spaceshooter
        sleep 2
        adb shell am start -n com.spaceshooter/.MainActivity
        sleep 5
        
        echo ""
        echo "6. Simulating touch to start game (tap center)..."
        adb shell input tap 540 960
        sleep 3
        
        echo ""
        echo "7. Simulating gameplay (swipe)..."
        adb shell input swipe 540 960 540 800 100
        sleep 2
        
        echo ""
        echo "8. Checking for runtime errors..."
        adb logcat -d -t 50 > ./runtime_logcat.txt
        ERRORS=$(grep -i "E/" ./runtime_logcat.txt | grep -v "AudioFlinger\|SurfaceFlinger\|OpenSL\|EGL_adreno" | head -5)
        if [ -z "$ERRORS" ]; then
          echo "   ✓ PASS: No runtime errors"
        else
          echo "   ⚠ WARNING: Errors found (may be non-critical):"
          echo "$ERRORS"
        fi
        
        echo ""
        echo "=========================================="
        echo "ORIENTATION TEST"
        echo "=========================================="
        
        echo ""
        echo "9. Testing orientation change..."
        adb shell settings put system user_rotation 1
        sleep 2
        adb shell settings put system user_rotation 0
        echo "   ✓ PASS: Orientation test complete"
        
        echo ""
        echo "=========================================="
        echo "ALL TESTS PASSED!"
        echo "=========================================="
    
    - name: Capture final screenshot
      if: always()
      run: |
        adb shell screencap -p /sdcard/final_screen.png
        adb pull /sdcard/final_screen.png ./
        adb logcat -d -t 200 > ./e2e_logcat_full.txt
    
    - name: Upload test artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: e2e-test-results
        path: |
          ./screen.png
          ./final_screen.png
          ./logcat.txt
          ./runtime_logcat.txt
          ./e2e_logcat_full.txt
        retention-days: 7
    
    - name: Cleanup
      if: always()
      run: |
        echo "Cleaning up..."
        adb emu kill 2>/dev/null || true

  # =========================================
  # JOB 3: Create Release (on tag push)
  # =========================================
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Download release APK
      uses: actions/download-artifact@v4
      with:
        name: apk-release
    
    - name: Download debug APK
      uses: actions/download-artifact@v4
      with:
        name: apk-debug
    
    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT
    
    - name: Rename APK for release
      run: |
        if [ -f app/build/outputs/apk/release/app-release-unsigned.apk ]; then
          cp app/build/outputs/apk/release/app-release-unsigned.apk "SpaceShooter-${{ needs.build.outputs.version }}-unsigned.apk"
        fi
        cp app/build/outputs/apk/debug/app-debug.apk "SpaceShooter-${{ needs.build.outputs.version }}-debug.apk"
        ls -la *.apk
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          SpaceShooter-${{ needs.build.outputs.version }}-unsigned.apk
          SpaceShooter-${{ needs.build.outputs.version }}-debug.apk
        name: "Space Shooter ${{ needs.build.outputs.version }}"
        body: |
          ## Release ${{ needs.build.outputs.version }}
          
          ### Downloads
          - **Release APK (unsigned)**: `SpaceShooter-${{ needs.build.outputs.version }}-unsigned.apk`
          - **Debug APK**: `SpaceShooter-${{ needs.build.outputs.version }}-debug.apk`
          
          ### Build Info
          - **Date**: ${{ steps.date.outputs.date }}
          - **Commit**: ${{ github.sha }}
          - **Workflow**: [${{ github.workflow }} #${{ github.run_number }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
          
          ---
          Built automatically by GitHub Actions.
        draft: false
        prerelease: ${{ contains(github.ref, 'beta') || contains(github.ref, 'alpha') || contains(github.ref, 'rc') }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # =========================================
  # JOB 4: Deploy Website (on push to website/)
  # =========================================
  deploy-website:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, 'website') || github.event_name == 'workflow_dispatch'
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    concurrency:
      group: "pages"
      cancel-in-progress: false
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: 'website'
    
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
